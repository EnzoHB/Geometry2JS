import * as Drawers from '../wrapped.js';

class Drawer {
    constructor(element, context) {
        this.context = context; 
        this.element = element;
    };

    get ctx() {
        return this.context;
    };

    vector(canvas, vector) {
        Drawers.vector()
    };

    segment(segment) {

    };

    point(vector) {

    };

    circle(circle) {

    };
    
    clear() {

    };

    cartesian() {

    };

    background() {

    };
};